<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodel="clr-namespace:MauiAppUIDemo.ViewModels"
    x:Class="MauiAppUIDemo.LoginPage"
    x:DataType="viewmodel:LoginViewModel"
    BackgroundColor="#F3F3F3"
    Padding="30">

    <VerticalStackLayout Spacing="20" VerticalOptions="CenterAndExpand">

        <Label Text="Đăng nhập" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" />

        <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
        <Entry Placeholder="Mật khẩu" Text="{Binding Password}" IsPassword="True" />

        <Label Text="{Binding ErrorMessage}" TextColor="Red" FontSize="13" IsVisible="{Binding IsError}" />

        <Button Text="Đăng nhập"
                Command="{Binding LoginCommand}"
                BackgroundColor="#6200EA"
                TextColor="White"
                CornerRadius="8" />

        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

    </VerticalStackLayout>
</ContentPage>
